# Generated outputs (regenerable cache — never source)
results/

# Study results data — regenerable (traces are large; DB + .mat are outputs).
# The analysed results live in the study .md docs, which ARE tracked.
studies/**/*_raw/
studies/**/*.db
studies/**/*.mat
studies/**/results_tables.md   # regenerable intermediate (report_effective_inertia); results live in results_*.md

# Vendor reference PDFs / source docs (reference inputs, not repo source)
studies/**/source_docs/
# ...except effective_inertia, whose source_docs are tracked deliberately (the
# formulation-under-test + reference set the study validates against).
!studies/effective_inertia/source_docs/
!studies/effective_inertia/source_docs/**

# Simulink build/compile caches
**/slprj/
*.slxc

# MATLAB editor backups
*.asv
*.autosave
*.m~

# OS cruft
.DS_Store
Thumbs.db
